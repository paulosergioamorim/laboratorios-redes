1. Qual é o endereço Ethernet (48 bits) da sua máquina?

d0:94:66:d5:ac:06

2. Qual é o endereço de destino (48 bits) no quadro Ethernet que contém a requisição
HTTP GET? Este é o endereço Ethernet de gaia.cs.umass.edu? Se não, a que
dispositivo corresponde este endereço?

00:0c:29:ae:ad:cf

Não, é o endereço MAC do Gateway da rede privada

3. Qual o valor hexadecimal do campo de dois bytes Frame type (tipo do quadro) no
quadro Ethernet que carrega a requisição HTTP GET? A que protocolo de camada
superior isto corresponde?

0x8864. PPPoE Session

4. A partir do início do quadro Ethernet (não contar preâmbulo), a quantos bytes
aparece a letra ASCII “G” de "GET"no conteúdo do quadro?

66o byte sem preâmbulo

5. Qual o valor do endereço Ethernet de origem neste quadro? Este é o endereço da
sua máquina ou de gaia.cs.umass.edu? A que dispositivo corresponde?

00:0c:29:ae:ad:cf. Gateway da rede privada

6. Qual é o endereço de destino no quadro Ethernet? Este é o endereço Ethernet da
sua máquina?

d0:94:66:d5:ac:06. Sim

7. Qual o valor hexadecimal do campo de dois bytes Frame type neste quadro? Que
protocolo de camada superior isto indica?

0x8864. PPPoE Session

8. A quantos bytes do início do quadro Ethernet aparece a letra ASCII “O” da resposta
"OK"(código HTTP)?

A resposta não 200 OK, mas sim 301 Moved Permamently

9. Quantos quadros Ethernet (cada um contendo um datagrama IP com segmento
TCP) transportam dados que pertencem à resposta HTTP "OK 200 ..."completa?

2 (1 para request e 1 para response)

11. Quantas entradas estão armazenadas no cache ARP da sua máquina?

4

12. O que é exibido em cada entrada mostrada pelo comando arp -a?

| ip | mac address | protocol | network card |

### A partir daqui foi feito em outro lugar...

12'. Qual é o valor hexadecimal do endereço de origem no quadro Ethernet que contém
a requisição ARP enviada pela sua máquina?

74:fe:ce:ed:ca:38

13. Qual é o valor hexadecimal do endereço de destino no quadro Ethernet que contém
a requisição ARP? A que dispositivo corresponde este endereço (cliente, servidor,
roteador, switch, broadcast, etc.)?

ff:ff:ff:ff:ff:ff. Broadcast

14. Qual o valor hexadecimal do campo de dois bytes Frame type neste quadro? A que
protocolo de camada superior isto corresponde?

0x0806. ARP

15. A partir do início do quadro Ethernet, a quantos bytes começa o campo opcode da
mensagem ARP?

- ARP começa no byte 14
- Opcode começa no byte 20
- 6 bytes

16. Qual é o valor do campo opcode na requisição ARP enviada pela sua máquina?

0x0001 (request)

17. A requisição ARP contém o endereço IP do remetente? Se sim, qual é esse valor?

Sim. 192.168.70.1

18. Qual é o endereço IP cujo correspondente endereço Ethernet está sendo solicitado
na requisição ARP da sua máquina?

192.168.70.4

19. Qual é o valor do campo opcode na mensagem de resposta ARP recebida pela sua
máquina?

0x0002 (reply)

20. Qual é o endereço Ethernet correspondente ao endereço IP que foi solicitado na
requisição ARP (resposta obtida)?

3c:7c:3f:3b:93:4c

# Discussão adicional

21. No traço de captura podem existir requisições ARP originadas por outros disposi-
tivos da rede. Por que não vemos respostas ARP para essas requisições no nosso traço? Explique.

(???)

# Exercícios Complementares

EX-1. O comando arp -s InetAddr EtherAddr permite inserir manualmente entradas
estáticas no cache ARP. O que ocorreria se fosse inserido o IP correto, porém o
endereço Ethernet incorreto? (Pista: considere ataques de segurança conhecidos
como ARP poisoning ou ARP spoofing.)

EX-2. Qual é o tempo padrão que uma entrada permanece na sua cache ARP antes de
expirar? Determine-o empiricamente ou consultando a documentação do sistema
operacional e indique como obteve o valor.

